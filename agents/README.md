# Agents

One folder per agent. An agent is a worker that uses the brain to do a job.

The agent's code lives here. Cloud agents *run* on Railway, with data in Supabase — this folder holds the code, not the running process.

Each agent reads from `../Brain 2/wiki/` — the shared files plus the relevant person's folder.

## Agents in this repo

- (none yet)
